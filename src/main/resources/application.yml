spring:
  profiles:
    include: db

  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDB103Dialect
    show-sql: true

# ?? ???? ???? ??
logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace